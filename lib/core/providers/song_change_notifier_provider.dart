import 'package:flutter/material.dart';

class SongChangeNotifier extends StatelessWidget {
  const SongChangeNotifier({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
